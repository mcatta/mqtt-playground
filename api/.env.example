# Node Environment
NODE_ENV=production
PORT=3000

# Database Configuration (same as MQTT logger)
# For Railway: Reference existing MySQL service with {{MySQL.VARIABLE}}
DB_HOST=localhost
DB_PORT=3306
DB_USER=meshtastic
DB_PASSWORD=meshtastic
DB_DATABASE=meshtastic

# JWT Configuration
# IMPORTANT: Generate a strong random secret (minimum 32 characters)
# You can use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-change-this-in-production
JWT_EXPIRES_IN=3600
JWT_REFRESH_EXPIRES_IN=604800

# CORS Configuration
# For production: Set to your frontend domain (e.g., https://yourdomain.com)
# For development: Use * to allow all origins
CORS_ORIGIN=*

# Initial Admin User (optional)
# Default username: admin, password: admin (if not specified)
INITIAL_ADMIN_PASSWORD=admin
