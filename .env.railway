# Railway Environment Variables
# Copy these to Railway dashboard for each service

# ============================================
# MESHTASTIC LOGGER SERVICE
# ============================================

# MQTT Broker Configuration
# Use the internal service name that Railway assigns
MQTT_BROKER=mqtt://mqtt-broker.railway.internal:1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=meshtastic-logger

# Meshtastic Topic Configuration
MESHTASTIC_ROOT_TOPIC=msh/US/#

# Database Configuration
# Railway automatically provides these via MySQL plugin
# Reference the MySQL service using Railway's variable syntax
DB_HOST=${{MySQL.MYSQLHOST}}
DB_PORT=${{MySQL.MYSQLPORT}}
DB_USER=${{MySQL.MYSQLUSER}}
DB_PASSWORD=${{MySQL.MYSQLPASSWORD}}
DB_DATABASE=${{MySQL.MYSQLDATABASE}}

# Or use DATABASE_URL if preferred (Railway provides this automatically)
# DATABASE_URL=${{MySQL.DATABASE_URL}}

# Node Environment
NODE_ENV=production

# TLS Configuration (optional)
MQTT_USE_TLS=false
MQTT_PSK_KEY=
MQTT_PSK_IDENTITY=

# ============================================
# MQTT BROKER SERVICE
# ============================================
# No environment variables needed for basic setup
# Add these only if you enable authentication:

# MOSQUITTO_USERNAME=admin
# MOSQUITTO_PASSWORD=your_secure_password

# ============================================
# PUBLIC ACCESS INFORMATION
# ============================================
# After deployment, Railway will assign:
# - TCP Proxy URL (e.g., monorail.proxy.rlwy.net:12345)
# - Use this to connect your Meshtastic devices from anywhere
